ПРОМТ ДЛЯ НОВОГО ЧАТА: "Настройка MQTT и ESP32 для мониторинга температуры"
Контекст проекта
Мы настроили полную IoT-систему мониторинга температуры на базе ESP32 и MQTT. Вот что уже сделано:

✅ ВЫПОЛНЕНО:
MQTT брокер Mosquitto установлен и работает на Windows

Сервис: Mosquitto Broker

Путь: C:\Program Files\mosquitto\

IP: 192.168.0.199, порт: 1883

Команды mosquitto_sub и mosquitto_pub доступны в PowerShell

Тестовая среда проверена:

Брокер принимает и отправляет сообщения

PowerShell утилиты работают корректно

ESP32 подготовлен с кодом для:

Подключения к WiFi (SSID: GalaxyM, пароль: www111www)

Чтения температуры с датчика DS18B20 (пин GPIO4)

Отправки JSON-данных в MQTT брокер

📋 КОД ESP32 (последняя версия):
cpp
#include <WiFi.h>
#include <PubSubClient.h>
#include <OneWire.h>
#include <DallasTemperature.h>

const char* ssid = "GalaxyM";
const char* password = "www111www";
const char* mqtt_server = "192.168.0.199";

#define ONE_WIRE_BUS 4
OneWire oneWire(ONE_WIRE_BUS);
DallasTemperature sensors(&oneWire);

WiFiClient espClient;
PubSubClient client(espClient);

void setup_wifi() {
  WiFi.begin(ssid, password);
  while (WiFi.status() != WL_CONNECTED) delay(500);
}

void reconnect_mqtt() {
  while (!client.connected()) {
    String clientId = "ESP32-Temp-" + String(random(0xffff), HEX);
    if (client.connect(clientId.c_str())) {
      client.publish("home/esp32/status", "{\"status\":\"connected\"}");
    } else delay(5000);
  }
}

void setup() {
  Serial.begin(115200);
  sensors.begin();
  setup_wifi();
  client.setServer(mqtt_server, 1883);
}

void loop() {
  if (!client.connected()) reconnect_mqtt();
  client.loop();
  
  static unsigned long lastSend = 0;
  if (millis() - lastSend > 10000) {
    lastSend = millis();
    sensors.requestTemperatures();
    float tempC = sensors.getTempCByIndex(0);
    
    String payload = "{";
    payload += "\"temperature\":" + String(tempC, 2);
    payload += ",\"unit\":\"C\"";
    payload += ",\"sensor\":\"DS18B20\"";
    payload += ",\"timestamp\":" + String(millis() / 1000);
    payload += "}";
    
    client.publish("home/sensors/temperature", payload.c_str());
  }
  delay(100);
}
🎯 ТЕКУЩАЯ ЗАДАЧА:
Загрузить код на ESP32 и начать получать данные температуры в MQTT

🔧 КОМАНДЫ ДЛЯ ПРОВЕРКИ:
powershell
# 1. Проверить работу брокера
mosquitto_sub -h localhost -t "#" -v

# 2. Отправить тестовое сообщение
mosquitto_pub -h localhost -t "test/topic" -m "Hello MQTT"

# 3. Слушать только температуру
mosquitto_sub -h localhost -t "home/sensors/temperature" -v
📊 ПЛАНИРУЕМЫЕ СЛЕДУЮЩИЕ ШАГИ:
Визуализация данных (Node-RED/Grafana)

Автоматизация (включение вентилятора при t>26°C)

Мобильные уведомления

История измерений в базе данных

❓ ВОПРОСЫ ДЛЯ ПРОДОЛЖЕНИЯ:
ESP32 не подключается к WiFi/MQTT

Данные не приходят в брокер

Настройка автоматизации в Node-RED

Создание веб-интерфейса для мониторинга

КОРОТКАЯ ВЕРСИЯ ДЛЯ БЫСТРОГО СТАРТА:
text
ESP32 + DS18B20 → WiFi → MQTT брокер (192.168.0.199:1883) → Python/Node-RED → Автоматизация
Статус: MQTT брокер настроен, код для ESP32 готов. Нужно загрузить прошивку на ESP32 и начать получать данные.

💡 Совет для нового чата: "Начни с проверки подключения ESP32 к WiFi и MQTT. Используй монитор порта в Arduino IDE для отладки. Если данные приходят, настроим автоматизацию на Python."

